using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProjectStartScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PrincipalManager principalManager = PrincipalManager.instance;
    }
}
